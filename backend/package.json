{
  "name": "stock-watcher-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "vitest --typecheck --run",
    "cf-typegen": "wrangler types",
    "db:migrate": "wrangler d1 migrations apply stock-watcher-db",
    "db:migrate:local": "wrangler d1 migrations apply stock-watcher-db --local"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "@stock-watcher/shared": "*",
    "fflate": "^0.8.2",
    "hono": "^4.6.0",
    "iconv-lite": "^0.7.1",
    "openai": "^6.15.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.11.1",
    "vitest": "~3.2.0",
    "wrangler": "^4.0.0"
  }
}
